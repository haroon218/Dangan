import { HighlightWeekDirective } from './highlight-week.directive';

describe('HighlightWeekDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightWeekDirective();
    expect(directive).toBeTruthy();
  });
});
